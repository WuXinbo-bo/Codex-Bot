const test=require('node:test'),assert=require('node:assert/strict'),P=require('../shared/completion-policy.cjs');
test('defaults retain reminders and escalating thresholds are deterministic',()=>{const i={receivedAt:1000};assert.equal(P.evaluate(i,{},1e9).hidden,false);for(const [age,stage]of [[119999,0],[120000,1],[300000,2],[600000,3],[1200000,4]])assert.equal(P.evaluate(i,{completionEscalation:'angry'},1000+age).stage,stage);});
test('unconfirmed completions never auto-close and policy never mutates inbox',()=>{const i={id:'a',receivedAt:1000};assert.equal(P.evaluate(i,{autoCloseCompletions:true,completionCloseMinutes:1},1e9).hidden,false);assert.deepEqual(i,{id:'a',receivedAt:1000});});
test('settings clamp invalid values and cap escalation',()=>{assert.equal(P.normalize({completionCloseMinutes:-1}).completionCloseMinutes,15);assert.equal(P.evaluate({receivedAt:1},{completionEscalation:'off'},1e9).stage,0);assert.equal(P.evaluate({receivedAt:1},{completionEscalation:'gentle'},1e9).stage,1);});
